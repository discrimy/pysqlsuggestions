# File-based authentication: the simplest authenticator Trino ships, and enough
# to answer the only question these tests ask — whether the Authorization header
# the reader builds is one Trino accepts.
password-authenticator.name=file
file.password-file=/etc/trino/password.db
